/**
 * Core Services Index
 * 
 * Centralized exports for all core services.
 */

export { FavoritesStore, favoritesStore, MutationQueue, mutationQueue, OfflineCache, offlineCache } from './offline-cache';
export type { CacheEntry, Movie, QueuedMutation } from './offline-cache';

